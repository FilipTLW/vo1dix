# Vo1dix
Vo1dix is a hobby OS created mainly for learning purposes. It runs on i386 architecture and can be booted using GRUB.

## Dependencies
* GCC i686 cross compiler
* nasm
* make
* grub
* qemu

## Building and running
The ISO and headers can be built using `make`. You can also directly run the OS in qemu using `make run`.